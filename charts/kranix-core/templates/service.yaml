apiVersion: v1
kind: Service
metadata:
  name: {{ include "kranix-core.fullname" . }}
  labels:
    {{- include "kranix-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "kranix-core.selectorLabels" . | nindent 4 }}
